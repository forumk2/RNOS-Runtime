runtime:
  max_steps: 5

policy:
  refuse_entropy: 6.0
  refuse_trust: 0.3
  degrade_entropy: 3.5
  degrade_trust: 0.5

planner:
  use_lm_studio: false
  endpoint: "http://127.0.0.1:1234/v1/chat/completions"
  model: "local-model"
